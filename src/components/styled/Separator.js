import React from 'react';
import Styled from 'styled-components';
import { borderGray } from './const.js';

export default Styled.hr`
  border: 0;
  height: 0;
  margin: 30px 0;
  border-top: 1px solid rgba(0, 0, 0, 0.1);
`;
