import React from 'react';
import Styled from 'styled-components';
import { borderGray, footerHeight } from './const.js';

export default Styled.footer`
  height: ${footerHeight};
  border-top: ${borderGray};
  padding: 65px;
`;
